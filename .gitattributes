# Shell scripts and configs must keep LF endings even when the repo is
# cloned on Windows — CRLF would break them inside WSL.
*.sh   text eol=lf
*.conf text eol=lf
*.md   text eol=lf
*.yml  text eol=lf
*.ps1  text eol=crlf
